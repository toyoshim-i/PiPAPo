/*
 * <sys/mount.h> — mount flags (MS_RDONLY, …) and umount2 flags.
 *
 * Forwards to src/common/mount.h.
 */

#ifndef _SYS_MOUNT_H
#define _SYS_MOUNT_H

#include "common/mount.h"

#endif /* _SYS_MOUNT_H */
