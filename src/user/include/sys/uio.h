/*
 * <sys/uio.h> — struct iovec for scatter/gather I/O.
 *
 * Forwards to src/common/iovec.h (POSIX places struct iovec here, so
 * the consumer-facing name differs from the internal filename).
 */

#ifndef _SYS_UIO_H
#define _SYS_UIO_H

#include "common/iovec.h"

#endif /* _SYS_UIO_H */
